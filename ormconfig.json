{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "hitmarket",
  "synchronize": true,
  "logging": "all",
  "cache": true,
  "logger": "file",
  "entities": ["apollo/entity/**/*.ts"],
  "migrations": ["apollo/migration/**/*.ts"],
  "subscribers": ["apollo/subscriber/**/*.ts"],
  "cli": {
    "entitiesDir": "apollo/entity",
    "migrationsDir": "apollo/migration",
    "subscribersDir": "apollo/subscriber"
  }
}
